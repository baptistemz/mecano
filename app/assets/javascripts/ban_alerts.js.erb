//Avoid carquery alerts

alert = function(text) {
  console.log( 'tried to alert: ' + text )
};

//Avoid react_on_rails console.logs

if ("<%=Rails.env%>" === ("staging" || "production")){
  console.log = function(data){
    if(data.startsWith("RENDERED")){
      console.log("");
    }
  };
}
